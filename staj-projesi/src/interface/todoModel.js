/**
 * PROJE VERİ MODELİ (INTERFACE)
 * * Bizim Todo objemiz şu yapıda olacak:
 * {
 * id: Number,      // Benzersiz kimlik (örn: 1, 2, 158...)
 * text: String,    // Görevin içeriği (örn: "Markete git")
 * isCompleted: Boolean // Yapıldı mı? (true/false)
 * }
 */
export const exampleTodo = {
    id: 1,
    text: "Örnek Görev",
    isCompleted: false
};